import 'package:flutter/material.dart';

class YearlyReport extends StatelessWidget {
  const YearlyReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: [
          Text("PLACEHOLDER TEXT YEARLY")
        ],
      ),
    );
  }
}
